#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * _isdigit - check is a characters is a digit
 * @c: the character to be checked
 *
 * Return: 1 if c is a digi and 0 otherwise
 */
int  _isdigit(int c)
{

	return (isdigit(c));
}
